# Set these as Vercel Project → Settings → Environment Variables (not committed).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Make up your own passphrase — this is what gates who can submit storylines.
QUEUE_SECRET=
